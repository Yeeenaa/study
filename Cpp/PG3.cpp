#include <string>
#include <vector>
#include <iostream>
using namespace std;

vector<string> solution(vector<string> strings, int n) {
	vector<string> answer;
	cout << answer[0];
	return answer;
}
int main() {
	solution({ "sun", "bed", "car" }, 1);
}